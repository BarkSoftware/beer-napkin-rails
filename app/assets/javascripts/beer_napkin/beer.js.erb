var beer = beer || {
  version: "<%= Settings.client_version %>",
  assets: [],
  options: {
    stroke_color: '#001966',
    background: "<%= asset_url('napkin.png') %>",
    fontFamily: 'Amatic SC',
    menu: {
      width: 200,
      height: $(window).height() - 80
    },
    napkin: {
      width: function() { return $(window).width() - 200 },
      height: function() { return $(window).height() - 61 },
    },
    bottle: {
      height: 80,
      width: $(window).width()
    }
  }
};
